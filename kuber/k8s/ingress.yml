apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kuber-server-ingress
  labels:
    app.kubernetes.io/name: kuber-server-ingress
spec:
  ingressClassName: nginx
  rules:
   - http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: kuber-server-service
            port: 
              number: 80
