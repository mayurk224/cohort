services:
  client:
    build: client
    ports:
      - 5173:5173
    volumes:
      - ./client:/app
    command: npm run dev

  server:
    build: server
    ports:
      - 3000:3000
    volumes:
      - ./server:/app
    command: npx nodemon -L server.js

volumes:
  client:
  server:
